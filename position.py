class Position:
    
    def __init__(self, x, y, z):
        """
        Class to represent a position. All args are converted to float.
        """
        self.x = float(x)
        self.y = float(y)
        self.z = float(z)
